# Stellar Pay MVP

Two deployed Soroban contracts and a small web chat/payment MVP.

```bash
npm install
cp backend/.env.example backend/.env
npm run dev:api
npm run dev:web
```

Use Freighter on Stellar testnet. Amounts are XLM. Chat data is process-memory only.
